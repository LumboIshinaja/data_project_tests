[pytest]
markers =
    schema: Tests related to schema validation
    integrity: Tests related to data integrity
    business_logic: Tests validating business rules on data
    performance: Query performance and execution time checks
    raw_data: Tests related to the view with the raw data